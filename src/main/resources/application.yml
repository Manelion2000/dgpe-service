server:
  port: 8080   # par défaut, chaque profil peut le surcharger

spring:
  application:
    name: accord-Service
  profiles:
    active: prod  # profil par défaut

  # Configuration du service mail
  mail:
    host: smtp.gmail.com
    port: 587
    username: alertesmae@gmail.com
    password: 'fevh duqq bksu zlcw'
    protocol: smtp
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.ssl.trust: smtp.gmail.com
    test-connection: false

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 20MB


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

# Configuration personnalisée commune
app:
  storage:
    path: ./datas
  security:
    attempts:
      timeout: 5   # minutes
      max: 5       # tentatives max
